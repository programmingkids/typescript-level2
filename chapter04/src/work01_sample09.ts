export {};

// Arrayクラスを利用したstringの配列
const fruits: Array<string> = new Array<string>('apple', 'orange', 'melon');
console.log(fruits);

// Arrayクラスを利用したnumberの配列
const numbers: Array<number> = new Array<number>(10, 20, 30);
console.log(numbers);
